#!/bin/sh
# Remove Co-authored-by lines — do not attribute AI tooling or trailers on commits.
msgfile=$1
[ -n "$msgfile" ] && [ -f "$msgfile" ] || exit 0
tmp=$(mktemp "${TMPDIR:-/tmp}/commit-msg.XXXXXX")
grep -viE '^co-authored-by:' "$msgfile" > "$tmp" || true
mv "$tmp" "$msgfile"
